# Plugins to activate alongside hypeinbox for the characterization test
# suite. The declarative form exists because hypeinbox's manifest.xml still
# lists hypeAutocomplete / hypeLists as 3.x-era deps whose own transitive
# chains (hypeajax, hypecapabilities, hypestash) are still at 3.x baseline
# and fatal on Elgg 4.x bootCore (DI\object() was removed in PHP-DI 6.x).
#
# For the characterization suite we only need hypelists active — its
# 4.x-migrated branch loads cleanly and is enough to satisfy hypeinbox's
# internal hypeJunction\Access\EntitySet cross-plugin references.
#
# Order: top-of-file activates first.
hypelists
