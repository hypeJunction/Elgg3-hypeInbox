# Per-plugin test stack for Elgg 5.x.
#
# Run all commands from <plugin>/ (the parent of docker/):
#
#   docker compose -f docker/elgg5/docker-compose.yml up -d
#   docker compose -f docker/elgg5/docker-compose.yml exec elgg bash
#   docker compose -f docker/elgg5/docker-compose.yml --profile test run --rm node \
#     sh -c "cd /plugin/tests/playwright && npm ci && npx playwright test"

name: ${PLUGIN_ID:?set PLUGIN_ID in docker/elgg5/.env}-elgg5

services:
  elgg:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - elgg-data:/var/www/data
      - ../..:/var/www/html/mod/${PLUGIN_ID}
      - ../../../hypelists:/var/www/html/mod/hypelists:ro
    environment:
      PLUGIN_ID: ${PLUGIN_ID}
      ELGG_DB_HOST: db
      ELGG_DB_NAME: elgg
      ELGG_DB_USER: elgg
      ELGG_DB_PASS: elgg
      ELGG_SITE_URL: "http://localhost:${ELGG_PORT:-8581}/"
      ELGG_DATA_ROOT: "/var/www/data/"
      ELGG_DB_PREFIX: "elgg_"
      ELGG_ADMIN_EMAIL: "admin@example.com"
      ELGG_ADMIN_PASSWORD: "admin12345"
    ports:
      - "${ELGG_PORT:-8581}:80"
    depends_on:
      db:
        condition: service_healthy
    entrypoint: /usr/local/bin/elgg-install.sh

  db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: elgg
      MYSQL_USER: elgg
      MYSQL_PASSWORD: elgg
      MYSQL_ROOT_PASSWORD: root
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - db-data:/var/lib/mysql
    ports:
      - "${DB_PORT:-3581}:3306"

  node:
    image: mcr.microsoft.com/playwright:v1.49.0-noble
    volumes:
      - ../..:/plugin
    working_dir: /plugin
    environment:
      PLUGIN_ID: ${PLUGIN_ID}
      ELGG_BASE_URL: "http://elgg"
      ELGG_PORT: "80"
      ELGG_DB_HOST: db
      ELGG_DB_PORT: "3306"
      ELGG_DB_USER: elgg
      ELGG_DB_PASS: elgg
      ELGG_DB_NAME: elgg
    depends_on:
      - elgg
    profiles:
      - test

volumes:
  elgg-data:
  db-data:
